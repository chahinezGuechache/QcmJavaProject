Une variable encapsulée n'interagira avec le monde extérieur que via des méthodes de la classe
Une variable encapsulée peut interagir avec le monde extérieur directement
Une variable d'instance déclarée avec le modificateur de visibilité private n'est accessible qu'à partir des instances de la classe
La visibilité d'une variable et sa portée sont deux synonymes